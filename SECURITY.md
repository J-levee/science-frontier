# 安全说明（Security）

## 🔑 关于历史中的 API Key

项目早期开发阶段，曾有一个临时测试用的 DashScope API Key 被**内联**进部分静态页面
（commit `c236e31`，后已由 `6d5535a` 迁至服务端代理模式）。该 Key 为**轮换后的临时密钥，早已作废**，无滥用风险。

在开源整理时，已使用 `git filter-repo` 将仓库历史中该明文 Key 全部重写为占位符
`sk-REDACTED-SEE-SECURITY-md`，**当前公开仓库历史中不再包含任何明文密钥**。

## 🛡️ 当前版本的密钥策略（前端零密钥）

- 网站前端的 AI 科普问答**不**存储任何 API Key。
- 所有模型请求统一发往服务端代理 `https://ai.sciencefrontier.cn`（Cloudflare Worker）。
- 代理在服务端持有密钥，并按请求 `Origin` 做 CORS 白名单校验，避免被第三方盗用烧额度。
- 本地构建脚本如需调用 LLM（如 `scripts/` 下的翻译 / 补全脚本），从根目录 `.env` 读取密钥，
  `.env` 已被 `.gitignore` 忽略，**不会进入仓库**。请勿将 `.env` 提交。

## 🐛 漏洞报告

如发现安全相关问题，请**不要**在公开 Issue 中披露细节（尤其不要粘贴任何密钥），
通过仓库 Issues 私信或邮件联系维护者，我们会尽快处理。

## ✅ 自检清单（贡献前）

- [ ] 没有把任何 `sk-` / `AK-` / token / password 写进代码或提交。
- [ ] `.env` 等含密钥文件未被 `git add`。
- [ ] AI 相关请求都走代理，不在前端硬编码密钥。
