# 评审：三体星系视图 — 拖拽交互与引力增强

> 关联：用户反馈「星系中的星球不能拖动，也没有引力作用的效果」；红线元素（三恒星结构 / 7 行星 / 水滴探测器）不可破坏。
> 依据协作铁律：生产代码改动先出评审文档 + 用户签批。本文即评审对象。

## 1. 现状诊断（代码证据）

`renderGalaxy`（index.html 689–836 行）：
- 已用 `d3.forceSimulation(nodes)`（805 行），含 `forceLink / forceManyBody / forceX / forceY / forceCollide`。
- **节点无 `d3.drag()`**（拖拽仅存在于知识星座网络视图 1129 行 `.call(d3.drag())`）。
- 行星节点在力模拟结束后被 `forceX/forceY` 拉到 `tx/ty`（所属恒星方向）后静止（alpha 衰减到 0），**引力不可感知**；恒星 `fx/fy` 写死固定。

→ 用户体感：「星球不能拖动、没有引力效果」属实。

## 2. 改动范围（最小化，红线零破坏）

下述元素**全部保留不动**：三恒星 = 物理/生命/计算（结构、位置、颜色）、7 行星集合、水滴探测器（光标跟随 + 探针光）、5 色语义、标签文案、信任锚。仅增强交互层：

1. **行星可拖拽**：对 planet 节点追加 `.call(d3.drag())`，复用知识星座模式
   （`.on('start').on('drag').on('end')`）。拖拽时 `d.fx=e.x; d.fy=e.y` 跟随光标；松手释放 `fx/fy=null`。
2. **引力回弹（可见引力）**：松手后 `simulation.alphaTarget(0).restart()`，
   由 `forceX/forceY`（目标 = 所属恒星 `tx/ty`）把行星拉回轨道 —— 形成「引力把星球拽回」的可视效果。
   将 planet 的 `forceX/forceY` strength 由 `0.12` 提升至 `~0.22`，回弹更明显但不僵硬。
3. **恒星锚定**：三恒星 `fx/fy` 维持固定（结构稳定），不接 drag（避免星系骨架被拖散）。
4. **克制美学**：仅拖拽/松手瞬间产生引力运动，无持续抖动，符合 #22「克制科幻」基调。

## 3. 风险与对策

| 风险 | 对策 |
|---|---|
| 拖拽 vs 点击跳转 `showDomain` 冲突 | d3.drag 在无位移时仍触发 click，planet 点击进入领域不丢失；若实测误触，加位移阈值（>4px 判为拖拽，抑制 click） |
| 松手后回弹过头/震荡 | `alphaTarget(0)` + 现有 `forceCollide` 防重叠，回弹平滑收敛 |
| 触摸端拖拽 | d3.drag 原生支持 pointer 事件，移动端可直接拖；`touch-action:none` 已在容器层级确保 |

## 4. 验收标准

- [ ] 行星可被鼠标 / 触摸拖拽；松手后向所属恒星回弹（引力可见）。
- [ ] 三恒星结构、水滴探测器、配色、标签不变。
- [ ] 无新增 console 错误（headless 复测通过）。

## 5. 改动文件

- `website/index.html`：`renderGalaxy` 函数（约 776–836 行节点选择 + 力模拟区间）。

## 6. 非目标（防范围蔓延）

- 不重排星系结构、不改领域映射、不新增数据源。
- 不做「三体问题」真实 N 体物理模拟（仅视觉引力回弹，非科研级仿真）。
