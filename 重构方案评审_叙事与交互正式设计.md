# 重构方案评审 · 叙事与交互正式设计（科学边界）

> 状态：**草案 · 待用户签批**（签批前不动任何生产代码；依协作铁律）
> 对应规格：`06-Missing-Docs-Specs/A-前端重构_叙事与交互方案.md`
> 目标读者：重构实施 Agent（视觉叙事 + 前端架构）、用户（签批）
> 强制约束：8.13 初赛截止；兜底模型硬截止 **8.8** 评估重构是否达标

---

## 1. 体验现状诊断（基于代码核查）

核查对象：`website/index.html`（517KB 单文件，含已落地的 #22 动效层）。诊断结论（代码级，建议实施前再用 5 分钟录屏最终确认"断裂点"清单）：

| # | 断裂点 | 代码证据 | 影响 |
|---|--------|----------|------|
| D1 | **荣誉星图与主线脱节** | `<section id="honor-section">` 内 `.honor-card` 平铺网格，独立于星系视图；区块内自注"此板块为临时承载，前端重构后将被替换" | 用户原话"讲不好故事"的最大痛点：诺奖巨匠本应是叙事高潮，却沦为页面下方的孤立卡片墙 |
| D2 | **无滚动故事弧** | 三视图靠 `showGalaxy()/showNetwork()` 等 nav 按钮跳切；无 section 锚点、无滚动叙事流 | 读者在"切换标签"而非"经历旅程"，叙事不连贯 |
| D3 | **问题详情叙事弱** | 详情以字段罗列为主（description/current_state/related/sources） | 缺"为什么重要""历史突破/相关诺奖"的编织，读者不知该问题在人类知识地图上的位置 |
| D4 | **奖项↔问题单向不可达** | `renderHonor()` 仅渲染卡片，无反向链接；109 问题无 `laureate_links` 字段 | 从问题找不到相关获奖者，反之亦不通，违背规划"引力网"母题 |
| D5 | 首屏钩子/信任/引导 | #22 已落地（副文案 + 信任徽章 + 探索三步 + 卡片动效） | ✅ 已解决，重构**在此之上叠加**，不重做 |

> 结论：#22 已解决表层交互（D5）。真正待重构的是 **D1–D4 的"叙事编织"**，而非推倒重来。

---

## 2. 故事弧设计

把"标签跳切"升级为一段旅程，落到滚动节奏与视图编排：

```
已知边界(Hero·边界线 + 钩子 + 信任锚[含"N位诺奖灯塔"])
   ↓ 滚动
三体星系(物理/生命/计算 三恒星 + 7行星，水滴探测器穿梭)
   ↓ 滚动
荣誉星图(诺奖=灯塔，按三体着色，照亮未知之浩瀚)   ← 叙事高潮，替换 TEMP
   ↓ 滚动
知识星座(109 问题 D3 力导向关联网络，拖拽探索)
   ↓ 点击
问题详情(为什么重要 + 关键突破/相关诺奖 + 相关领域问题 + 100%溯源)
```

一句话可描述：**"从已知的边界出发，借三体星系与诺奖灯塔，照亮 109 个最前沿问题。"**

---

## 3. 交互骨架（信息架构）

保留现有三视图，**新增"荣誉星图"为第 4 视图**，并以深链接打通双向可达：

```
导航（粘性 tab，随滚动高亮当前段）
 ├ 星系视图      （现有 D3 力导向，水滴探测器）      — 不动结构
 ├ 荣誉星图  ★新  （诺奖灯塔星场，hover/click→详情）  — 替换 TEMP
 ├ 关联网络      （现有 D3 力导向 179 连线）          — 不动结构
 └ 列表视图      （现有领域/问题列表）               — 不动结构

深链接（双向可达）：
 问题详情 ←→ 荣誉星图：问题页"相关诺奖"块点击 → 跳荣誉星图并定位该获奖者；
                       获奖者详情"相关前沿问题"点击 → 跳问题详情。
 星系视图 ←→ 荣誉星图：同 domain 高亮联动（可选）。
```

> 不把 1482 个获奖者塞进问题星系（会爆炸）；独立星场 + 过滤（沿用现有 star/award/search 过滤 UI）更稳。

---

## 4. 视觉语言

- **继承**：5 色语义（`--accent` 已知/探索 · `--cool` 关联 · `--warm` 突破 · `--hot` 高难 · `--green` 可信）、深空星海风、#22 玻璃卡 + 滚动渐显 + 信任徽章 + 探索三步。
- **新增**：
  - 荣誉星图"灯塔"节点：按 `HONOR_STAR_COLOR = {physics:'#818cf8', life:'#10b981', compute:'#f59e0b'}` 着色（与现有一致），节点带柔和光晕（克制，不抢星系），尺寸按奖项权重（诺奖 > Fields/Turing）。
  - 星系背景极淡 aurora（呼应 #22 §10，置于星系之下、低透明度）。
- **动效原则**：克制、不喧宾夺主；窄屏降级直显。

---

## 5. 荣誉星图正式设计（替换 TEMP）

**节点 = 获奖者"灯塔"**，数据源 `awards-data.js`（1482 条，全局 `awards`）。

| 要素 | 规格 |
|------|------|
| 渲染 | D3 星场（已加载 D3），节点 = 获奖者；力导向或径向分布按三体聚类 |
| 着色 | `HONOR_STAR_COLOR` 三体系；光晕表"照亮未知" |
| hover | 显 `name` + `year` + `category` + `discovery`(中译获奖理由，截断) |
| click | 右侧/浮层详情：`discovery` 全文 + **相关前沿问题**(problemLinks) + 外部 `url` |
| 过滤 | 沿用现有 `honor-star`(星系) / `honor-award`(奖项) / `honor-search`(姓名/机构/国家) |
| Hero 锚 | 信任徽章增 `"N 位诺奖灯塔"`（计数 `awards` 中诺奖类），强化史诗感 |

**数据绑定与风险（关键）**：
- 当前 `awards-data.js` 需补两个字段才能撑起"叙事编织"：`discovery`(中文获奖理由) 与 `problemLinks`(关联 109 问题 ID 数组)。
- **problemLinks 映射**是主要排期风险：按"奖项类别 + discovery 关键词 → 三体 domain → 该 domain 下问题"做自动映射 + 人工抽检。
- **兜底**：若 8.8 前 problemLinks 未齐，则获奖者详情按 domain 展示该领域全部问题（仍可达，只是不够精准），不阻塞交付。
- 反向：109 问题 `frontier-data.yaml` 增 `laureate_links` 字段（由同一映射生成），问题详情加"关键突破/相关诺奖"块。

---

## 6. 技术选型决策

**维持零依赖单文件 + 离线优先**（推荐，除非用户拍板引入构建链）。

| 方案 | 收益 | 代价 | 结论 |
|------|------|------|------|
| 维持零依赖单文件 | 双击即跑、零部署、比赛"易传播"加分；`awards-data.js`(>300KB) 外链不内联，符合既有规则 | 单文件偏大、后续维护略繁 | **采用（8.13 前）** |
| 引入 Vite/框架 | 工程化、可维护 | 破坏"双击即跑"、需构建链、显著挤占 12 天排期 | 赛后再评估 |

---

## 7. 实施分阶段（Tier 1 必达 / Tier 2 拉伸）

### Tier 1 — 初赛必达（D1–D8，硬截止 8.8）
- **D1–2 数据 enrichment**：`awards-data.js` 补 `discovery`(中译) + `problemLinks`；`frontier-data.yaml` 补 `laureate_links`，重生成 JSON。（风险点，备 domain-only 兜底）
- **D3–5 荣誉星图视图**：D3 星场 + hover/click 详情 + 过滤；替换 TEMP `#honor-section`。
- **D3–5 故事弧编排**：section 锚点 + 粘性 nav 随滚动高亮 + 滚动渐显（#22 已有，编排进弧线）。
- **D6 问题详情叙事块**：加"为什么重要" + "关键突破/相关诺奖" + "相关领域问题"。
- **D6–7 教学引导路径**（兼补比赛缺口①）："新手→进阶→深度"三路线 + 学习目标标签 + 每领域"为什么重要"科普卡。
- **D7 移动端叙事**：响应式 + 触摸可用。
- **D8 (8.8) 自测**：jsdom 零 JS 错误 + 双链可达 + 硬截止评估（达标→用重构版参赛；否则发当前稳定版）。

### Tier 2 — 拉伸（8.8 后 / 若进决赛）
- problemLinks 精准化（人工抽检扩容）；P2 研究者档案（OpenAlex + 策展）并入"现役探测器"；更丰富的 discovery 文本；若进决赛做 P3 文献关联。

---

## 8. 验收标准

- [ ] 故事弧可用一句话清晰描述，首屏 5 秒内传达定位（含诺奖灯塔锚）。
- [ ] 荣誉星图正式上线，替换 TEMP；获奖者↔问题**双向可达**。
- [ ] 移动端叙事可用（响应式 + 触摸）。
- [ ] 不破坏 100% 溯源锚与 5 色语义、边界线、三体星系、水滴探测器、知识星座。
- [ ] jsdom 真实渲染零 JS 错误。
- [ ] 用户签批通过。

---

## 9. 叙事保护红线 + 数据同源

**红线（绝不动）**：边界线/Boundary SVG · 5 色语义 · 三体星系结构 · 水滴探测器 · 100% 溯源锚 · 知识星座。荣誉星图是**新增/替换 TEMP**，不重复、不覆盖上述。

**数据同源策略**：`data/frontier-data.yaml` 为 109 问题唯一权威源；新增 `laureate_links` 在此定义，脚本重生成 `frontier-data.json/js` 与技能副本。`awards-data.js` 为获奖者权威源（enrichment 后）。保持"改 YAML → 重生成"单一链路，避免 03/04/08 三处副本过期（见交接 README §5.6）。

---

## 10. 风险与回退

| 风险 | 概率 | 缓解 |
|------|------|------|
| problemLinks 映射排期超 8.8 | 中 | domain-only 兜底；不阻塞交付 |
| 重构整体超 8.8 | 中 | 兜底模型：发当前稳定版参赛，重构赛后继续 |
| 单文件过大/性能 | 低 | awards-data.js 外链；星场节点数可分页/抽样 |
| 回退 | — | `index.html.bak` 在；Tier 1 各子项独立可回退；git 已建（master 可随时 checkout 初始提交） |

---

## 11. 签批栏

- [ ] 确认诊断 D1–D4（或补充录屏佐证）
- [ ] 确认故事弧（第 2 节）
- [ ] 确认交互骨架（第 3 节：新增荣誉星图视图 + 双向深链）
- [ ] 确认荣誉星图设计 + 数据 enrichment 范围（第 5 节，含兜底）
- [ ] 确认维持零依赖单文件（第 6 节）
- [ ] 确认 Tier 1 范围与 8.8 硬截止（第 7 节）
- [ ] 确认叙事红线与数据同源策略（第 9 节）

签批前不动任何生产代码。
