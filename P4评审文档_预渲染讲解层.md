# P4 评审文档 · 预渲染讲解层（热点方向 × 模型预烘焙）

> 配套：开源终态架构规划 `#23`、前端优化评审 `前端优化评审_参赛UI打磨.md`、参赛规划 `参赛规划_小有可为2026.md`
> 目标：对若干热点研究方向，用模型在**构建期**提前生成「交互讲解页 + 衍生视频」，提交静态产物；观众**零 key、零成本、即时看**，并复用科学边界的溯源品牌。
> 状态：**已签批 · 2026-08-01（用户回复"全部确认"）**。可实施。

---

## 1. 背景与目标

- 用户判断：科学边界当前 `laureates=[]`、探索者为空，且 8.13 初赛需要"即开即看、内容扎实"的演示。
- 核心思路（已与用户对齐）：对热点方向，**构建期**用模型把讲解"预渲染"成静态产物，而非依赖运行时 LLM。一次生成、无限次观看。
- 同时串联三件事：① 填站（立刻有内容）② 比赛 demo（零 key 演示）③ 对齐开源终态（= 开源三层架构的 layer① 零 key 静态站）。

---

## 2. 概念与流水线

```
热点选题(策展) ──▶ 模型生成(脚本+溯源+TTS+视觉, 构建期 key) ──▶ 产物烘焙(交互HTML + 视频mp4)
        │                                                                      │
        └── 从 109 问题 / 7 行星策展，关联已溯源来源                          ▼
                                                          静态托管(零 key · 仓库/CDN) ──▶ 探索者免费观看
```

- **生成只在构建期跑一次**，产物是静态文件，运行时零 key、零成本。
- **视频是交互讲解的衍生分发物**：同一份 LLM 脚本 + TTS 音频，套星系风动画导出 mp4，主站仍留可探索的交互版。

---

## 3. 首发决策（用户已确认 · 2026-08-01）

| # | 决策项 | 结论 |
|---|--------|------|
| 1 | 首发形态 | **交互界面首发 + 视频作衍生**（推荐） |
| 2 | 首发选题 | 从 7 行星挑 3-5 个热点（本神拍板清单见 §4） |
| 3 | 生成位置 | **v1 本地构建机用自有 key 预烘焙、提交产物**（零运行成本零风险） |
| 4 | 比赛绑定 | 把 3-5 个讲解烘焙成 8.13 初赛 demo 一起交（同意） |
| 5 | 视频渲染 | **TTS + 星系风动画**（便宜、贴品牌，推荐） |

---

## 4. 首发选题清单（本神拍板）

原则：覆盖三体三柱（物理/生命/计算），且优先选**数据已溯源强 + 国内/全球热度高**的方向。数据支撑来自 `data-inventory-report.md`（109 问题中 5★ 溯源覆盖 88%）。

### P0 首批（用户指定 3 个，必做）

| slug | 方向 | 映射领域 | 数据/溯源支撑 | 热度理由 |
|------|------|----------|--------------|----------|
| `fusion` | 可控核聚变 | 材料与能源（核聚变/四代核电） | 5★ 溯源清单含"核聚变"；国内"人造太阳"热点 | 近两年国内影响大 |
| `ai-alignment` | AI 对齐与智能 | 信息与智能（AGI / AI4Science） | INF-010 AI4Science、AGI 已在库 | 全球 #1 热点 + 用户指定 |
| `kakeya` | 挂谷猜想 / 数学前沿 | 数学与计算 | 数学域 12 题在库（含 P vs NP）；需策展 Clay/arXiv 权威源 | 2026 Fields Medal 中国热议，用户指定 |

### P1 扩量（全球营销大 + 中国突破，时间允许加）

| slug | 方向 | 映射领域 | 数据/溯源支撑 | 热度理由 |
|------|------|----------|--------------|----------|
| `quantum` | 量子计算与量子信息 | 物理学 / 数学与计算（量子计算） | 5★ 溯源清单含"量子计算"；九章/祖冲之突破 | 全球营销大 |
| `bci` | 脑机接口 | 信息与智能（INF-002 BCI） | INF-002 Nature Medicine 2024 BCI 已溯源 | Neuralink 全球营销大 |

- **8.13 demo 目标**：P0 三个必交；P1 两个有时间则一并烘焙，凑足 3-5 个。
- 用户可在签批时增删方向。

---

## 5. 形态与技术实现

### 5.1 交互讲解页（首发主形态）

- **每主题一个单文件 HTML**：`website/explainers/<slug>/index.html`，零依赖（纯 HTML/CSS/JS），与全站共用 5 色语义 + 星系视觉语言。
- **Guided tour 结构**（复用现有资产，非从零造）：
  1. 钩子：一句话说清"这是什么 + 为什么重要"
  2. 核心问题：从 109 关联问题卡片（带溯源链）铺开
  3. 关键进展 / 里程碑：时间线
  4. 主要玩家 / 研究者：预留接口，后续接 P2 研究者档案
  5. 开放问题 / 未解
  6. 溯源与延伸阅读：可点击权威链接
- **动效**：复用前端加法层（滚动渐显 / 玻璃卡 / 数字计数），统一质感（见 `前端优化评审_参赛UI打磨.md` §10）。
- **数据驱动**：每主题一份 `<slug>.json`（构建期 LLM 生成，含 narration 分段 + 关联问题 id + 权威来源），模板渲染成 HTML。

### 5.2 视频衍生（mp4）

- 同一 JSON 的 narration → **百炼 TTS**（cosyvoice / dashscope TTS）→ 音频。
- 星系风动画：用同一 HTML 页做"演示模式"（自动滚动 + 高亮），**headless 浏览器（Playwright/Puppeteer）录屏 + TTS 音频混流 → ffmpeg 合成 mp4**。
- 时长控制 **60-120s**；动画用 CSS/SVG（CPU 可渲，无 GPU 也行）。
- 若办公室电脑（无 GPU）录屏卡顿：降分辨率/帧率，或暂缓视频、先交交互版。

### 5.3 生成位置与工具链

- **本地构建机（办公室电脑）用自有百炼 key**，产物提交到仓库 `website/explainers/`。
- 运行时零 key、零成本。
- 工具脚本（纳入仓库 `science-frontier/build/`，非运行时依赖）：
  - `gen_explainer.py`：调 LLM（百炼/OpenAI 兼容）生成 `<slug>.json`
  - `render_html.py`：JSON → 单文件 HTML（套 `templates/explainer.html`）
  - `render_video.py`：HTML + 音频 → mp4（headless + ffmpeg）
- **API 可用性**：百炼 API 为服务端调用，不受浏览器 CORS 限制（此前 GFW/CORS 问题仅针对浏览器直连）。TTS 若百炼不可用，回退 Edge/微软/火山 TTS。

---

## 6. 产物与站点集成（加法，不碰叙事）

```
website/
  explainers/
    index.html              # 讲解总览（卡片网格，复用玻璃卡）
    kakeya/  fusion/  ai-alignment/  quantum/  bci/
      ├─ index.html         # 交互讲解页
      ├─ <slug>.json        # 构建期生成的结构化数据
      └─ <slug>.mp4         # 衍生视频（可选）
  index.html                # 主站：仅加一个「热点讲解 →」入口按钮；叙事核心不动
```

- 主站 `index.html` 仅**新增一个入口按钮**（呼应前端评审 §5-A），不改动星系/边界线/水滴/5 色/溯源锚/知识星座。
- 讲解区为**独立新增资产**，进入方式：主站 Hero 按钮 → `explainers/index.html`。

---

## 7. 溯源与可信（关键约束）

- 讲解页每条论断**必须关联 109 中已有 source 或策展权威源**（Clay / Nobel / Nature / arXiv / 官方机构），页面底部"溯源"区列出可点击链接。
- **绝不放无源断言**——否则砸"100% 溯源"招牌。
- 构建校验：`<slug>.json` 经 schema 校验 + **溯源率检查**（所有论断有 source）后方可渲染。

---

## 8. 比赛绑定（8.13 初赛）

- 3-5 个讲解页 = 初赛核心 demo 内容：零 key 即开即看、内容扎实、品牌统一。
- on-demand LLM 作为"未来高级功能"在答辩中讲述，不在 v1 交付。

---

## 9. 影响面与风险

- **影响面**：新增 `website/explainers/**` 静态资产 + `build/` 生成脚本（非运行时）+ 主站一个入口按钮。
- **不动**：主站叙事核心层（边界线/星系/水滴/5 色/溯源锚/知识星座）、`window.DATA` 数据层、D3 核心、后端。
- **风险与缓解**：
  - LLM 幻觉 → grounding + 人工抽检 + 溯源链接 + 构建期溯源率校验
  - 百炼 API 费用/波动 → 构建期一次性，成本低，失败可重试
  - 无 GPU 机器视频渲染慢 → 控时长/分辨率，或先交交互版
  - 与前端红线冲突 → explainers 为独立资产，主站叙事锁死（见 §11）

---

## 10. 验证方案

- 构建脚本：`<slug>.json` schema 校验 + 溯源率 100% 检查
- 生成 HTML：jsdom 零 JS 错误 + 本地 `http.server` 预览（宽/窄屏）
- 视频：播放检查（时长、音画同步）
- 截图对比（讲解页 vs 主站视觉一致性）

---

## 11. 明确不做（红线）

- 不改主站叙事核心：Hero 边界线、5 色语义、三体星系结构、水滴探测器、100% 溯源锚、知识星座/探针光（呼应前端评审 §11）。
- 不改 109 数据层 / D3 核心 / 后端。
- 不引入前端框架（保持零依赖单文件）。
- 不把讲解做成"需 key 才能看"（保持零 key 静态）。

---

## 12. 签批栏

- [x] 确认 §4 选题清单（P0 三 + P1 两，或增删）
- [x] 确认形态：交互首发 + 视频衍生
- [x] 确认生成位置：本地构建机自有 key 预烘焙
- [x] 确认 §7 溯源约束（讲解必须可溯源）
- [x] 确认 §6 集成方式（主站仅加入口按钮）
- [x] 确认先交付 P0 三个用于 8.13，P1 两个视时间

签批：**已批准 · 2026-08-01（用户回复"全部确认"）**。范围限定：P4 与 #22；P1 多奖项（#15）不自动扩大，仍待单独确认。
