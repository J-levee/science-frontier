# 科学边界 Science Frontier · 开源终态架构规划

> 版本：v1 · 2026-07-31
> 状态：规划（未改动任何代码，待评审签批后实施）
> 关联文档：视觉叙事方案 / P0评审 / P1评审（多奖项）/ P3规划（文献关联）/ 参赛规划（小有可为2026）/ 前端优化评审

---

## 1. 产品终态一句话

**科学边界是一个开源的、可自部署的前沿科学可视化知识平台：核心可视化（三体星系 + 诺奖/文献知识图）零后端依赖、双击即跑；AI 增强能力（探索者文献助手、方向解读）由用户自带大模型 key 驱动，算力与费用完全归用户自己。**

核心理念：**「可视化免费开放，AI 算力用户自带」**——既保证任何人零门槛能用，又让需要深度 AI 功能的探索者用自己的百炼/Qwen/OpenAI key 解锁，平台方不碰任何密钥、不承担任何算力成本。

---

## 2. 为什么必须「用户自带 key」（硬约束，来自外部检索）

三个检索结论决定了架构不能怎么设计：

| 结论 | 来源 | 对架构的影响 |
|------|------|------------|
| **百炼 DashScope 不支持浏览器直连** | 多家来源一致（CSDN Vue 集成、cnblogs、掘金 Vite 对接）确认：浏览器 fetch `dashscope.aliyuncs.com` 被 **CORS 拦截**，必须后端代理中转 | ❌ 不能「前端直连 + 用户 key 明文」 |
| **开源 AI 项目 key 存服务端** | LibreChat / Open WebUI / Libre WebUI 均为 MIT/自托管，key 存服务端 `.env` 或 per-user 加密，**前端永不接触真实 key** | ✅ 采用「用户自部署代理持有 key」模式 |
| **Cloudflare Worker 代理 = 零成本方案** | 多个 Worker 代理模板（HelloKimi / AI-Worker-Proxy / martinuke0 指南）证明：`wrangler secret put` 注入 key、不进 git、免费 100k 请求/天、且百炼 DashScope 提供 **OpenAI 兼容接口** | ✅ 提供开源 Worker 模板，用户一键部署 |

**结论**：任何「key 放前端」的方案在安全上不成立（key 会被扒、CORS 不通）。唯一干净路径是**用户自己部署一个最小代理，key 只在这代理里**。

---

## 3. 总体架构（三层 + 降级）

```
┌──────────────────────────────────────────────────────────┐
│ ① 浏览器（静态站点 · 零后端依赖）                          │
│   · 三体星系可视化（D3 纯前端）        —— 零 key 即用      │
│   · 诺奖/文献知识图（离线内联）       —— 零 key 即用      │
│   · 探索者 AI 功能（文献助手/方向解读）—— 需 key 才解锁   │
│        │（无 key → 该功能区锁定，提示「配置 key 解锁」）   │
└───────────────────────┬──────────────────────────────────┘
                         │  OpenAI 兼容请求（带用户自己的 token）
                         ▼
┌──────────────────────────────────────────────────────────┐
│ ② 用户自部署最小代理（Cloudflare Worker / 阿里云 FC）       │
│   · 开源模板，fork 后填 key 即部署                         │
│   · key 存 Secret（wrangler secret / FC 环境变量），不进 git│
│   · 转发 OpenAI 兼容请求到对应 provider，隐藏真实 key       │
│   · 免费额度 100k 请求/天（Cloudflare Free Plan）          │
└───────────────────────┬──────────────────────────────────┘
                         │ 持有真实 key
                         ▼
┌──────────────────────────────────────────────────────────┐
│ ③ LLM Providers                                           │
│   · 百炼 DashScope（Qwen）—— 推荐，与比赛同生态          │
│   · OpenAI 兼容端点（任意 OpenAI-compatible）             │
│   · 其他平台 key（DeepSeek / Gemini / 本地 Ollama 等）    │
└──────────────────────────────────────────────────────────┘
```

**降级逻辑（核心卖点）**：
- **无 key**：三体星系 + 诺奖/文献知识图 100% 可用（和现在一样），AI 功能区显示「配置 key 解锁」引导。
- **有 key**：AI 功能全开。前端把用户填的「代理地址 + 代理 token」存浏览器 localStorage（仅用户本机），每次请求走自己的代理。

---

## 4. 多 Provider 抽象层（前端）

前端抽象一个 `llmProvider` 配置：
- `baseURL`：用户代理地址（如 `https://my-proxy.workers.dev/v1` 或 FC 自定义域名）
- `apiKey`：用户代理的访问 token（非 provider key 本身，防滥用）
- `model`：如 `qwen-max` / `qwen-plus` / `gpt-4o` / 用户自选
- `provider`：百炼 / OpenAI / 其他（决定 system prompt 与参数微调）

默认预置「百炼（Qwen）」配置模板（含 DashScope OpenAI 兼容 base URL），其余 provider 用户填自定义即可。所有调用走 **OpenAI Chat Completions 兼容协议**，DASHSCOPE 已支持。

---

## 5. 文献检索层（去绑私有 SmartLib，改为可插拔）

此前 P3 规划把「实时文献检索」接我的私有 SmartLib。但终态要**开源对外开放**，不能绑死私有服务。调整为：

- **轨道 A · 离线策展（进 HTML，零依赖）**：每个问题/诺奖者的奠基、里程碑、综述文献，人工策展 + 预采集，内联成 `literatures-data.js`。与 P0 内联 D3、P1 内联 awards 完全同哲学。**承载「问题详情·奠基文献」「研究者档案·代表作」区块。**
- **轨道 B · 实时检索（可插拔 provider）**：抽象成 `searchProvider` 接口，默认留空/示例：
  - 公开源（arXiv / Semantic Scholar / OpenAlex / PubMed）——但**办公室电脑 GFW 阻断**，需用户代理中转（同 LLM 代理思路，或用户自有可访问环境）
  - 用户自有文献服务（如用户若部署了 global-biblio-base 开放 API、或 F 盘 CSV 自托管索引）
  - **不再默认依赖我的 SmartLib**；若未来我开放 SmartLib 公共 API，可作为可选 provider 文档示例。
- 复用 P3 的 `literatures` schema（与 `awards` 同构：`star`/`domain`/`linked_*`），不重复设计。

---

## 6. 开源仓库组织

```
science-frontier/                # GitHub 仓库（建议名）
├── LICENSE                      # MIT（与 LibreChat/OpenWebUI 一致，最友好）
├── README.md                    # 架构图 + 快速开始 + 部署指南 + 贡献指引
├── CONTRIBUTING.md
├── website/                     # 前端（当前 index.html 单文件 + assets）
│   ├── index.html               # 自包含单文件（可视化零依赖）
│   └── assets/                  # 拆分后的源（可选：开发态多文件，构建成单文件）
├── proxy/                       # 用户自部署最小代理模板
│   ├── worker.js                # Cloudflare Worker 版（wrangler.toml + 说明）
│   ├── fc-function/             # 阿里云函数计算版（与百炼同生态）
│   └── README.md                # 各平台 key 获取 + 一键部署步骤
├── scripts/                     # 数据采集脚本（P1 诺奖 / P3 文献策展）
│   ├── collect_awards.py
│   └── collect_literatures.py
└── docs/                        # 规划文档合集（本文件 + P0/P1/P3/参赛等）
```

**LICENSE 选 MIT**：最宽松、最利于开源传播与参赛展示（与主流开源 AI 项目一致）。
**README 必须含**：① 架构图（见本文 §3）② 三步快速开始（部署静态站 → 部署代理 → 填 key 解锁 AI）③ 各平台 key 申请链接 ④ 安全声明（key 只存在你自己的代理，平台方看不到）。

---

## 7. 部署方案

| 组件 | 推荐选项 | 备注 |
|------|---------|------|
| **静态前端** | GitHub Pages / Vercel / Netlify / **CloudStudio（阿里云自家，比赛加分）** | 单 HTML 文件，零构建 |
| **用户代理** | **Cloudflare Worker（零成本，海外快）** / **阿里云函数计算 FC（国内快，与百炼同生态，比赛加分）** | 用户 fork 后自行部署 |
| **自定义域名** | 可选，绑定后 CF 路由更优、延迟更低 | 文档指引 |

> 注：静态站部署在海外（GitHub Pages/Vercel）国内访问可能慢；部署在阿里云（CloudStudio/FC）国内快但需账号。两种都在 README 给方案，用户自选。

---

## 8. 隐私与责任边界

- **key 安全**：平台方（你我）**永不接触**用户 key。key 只存在用户自部署的代理 Secret 里 + 用户浏览器 localStorage（仅本机）。
- **费用自负**：AI 调用产生的所有算力费用由用户自己承担（走自己的百炼/OpenAI 账户）。
- **滥用防护**：代理模板默认带 `PROXY_AUTH_TOKEN`（用户自设），防止他人白嫖；文档说明改强。
- **合规**：开源协议 MIT；诺奖/文献数据注明来源与许可；可视化内容为用户策展，平台不主张版权。

---

## 9. 与比赛 / 当前进度的关系

- **初赛（8.13 截止）**：只需纯静态部署（P0 已零依赖单文件），**不必实现 key 架构**。但可在 README/文档/答辩里把「AI 增强靠用户自带 key」作为**产品愿景亮点**，体现架构前瞻性与合规意识。
- **终态落地路线**（初赛后逐步做）：
  ```
  现在 P0（可视化零依赖）✅
   → P1 多奖项采集（评审待签批）
   → P3 文献关联·策展（轨道 A）
   → 本终态：拆出 proxy/ 模板 + 前端 llmProvider 抽象 + README 开源化
   → P4 探索者文献台（轨道 B 接用户自有检索 provider）
  ```
- **比赛加分点**：开源 + MIT + 用户自带 key（合规、零成本扩张）+ 阿里云同生态（CloudStudio/FC/百炼）天然契合「AI 向善」赛道。

---

## 10. 待决策项（请你拍板）

1. **仓库名 / 组织**：用 `science-frontier` 还是更品牌化的名（如 `frontier-galaxy` / `three-body-science`）？
2. **代理默认模板优先哪个**：Cloudflare Worker（海外零成本）还是阿里云 FC（国内快、比赛加分）？建议**两者都给**，默认文档先写 CF。
3. **初赛要不要顺带做开源化**：仅补 README + LICENSE（低成本），还是等初赛后再整仓库？
4. **前端 llmProvider 配置 UI**：放在「设置」弹窗（存 localStorage）还是 URL 参数？建议设置弹窗。
5. **是否现在就拆 `website/` 成开发态多文件 + 构建单文件**：影响后续维护，但初赛可暂不动（保持单文件）。

---

## 11. 实施评审边界（动手前需另出 P_终态 评审文档）

本文为**产品形态规划**，落地时需另出评审文档，明确：
- 前端改动：新增 `llmProvider` 抽象 + 设置弹窗 + AI 功能区（仅前端 JS/CSS，不碰 `window.DATA`/D3 核心）
- 新增 `proxy/` 目录（独立文件，不影响 `index.html`）
- 回退：各子项独立可回退；`index.html.bak` 保留
- 验证：jsdom 验证无 key 降级 + 填 key 后 AI 功能调用路径（mock 代理）

> 按生产铁律：**本文仅规划，任何代码改动（含 proxy 模板、前端设置弹窗）须先出评审文档 + 你签批后实施。**

---

*附：本规划基于 2026-07-31 外部检索（百炼 CORS 约束、LibreChat/OpenWebUI 自托管 key 模式、Cloudflare Worker 代理实践）形成，检索结论见 §2 来源标注。*
