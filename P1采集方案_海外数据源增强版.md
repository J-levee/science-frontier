# P1 采集方案 · 海外数据源增强版（多奖项数据采集）

> 配套 P0（三体星系叙事已落地）。本次把 P0 里的 `const laureates = []` 占位升级为**真实多奖项数据采集**。
> 状态：**待用户签批**。签批前不动 `index.html`、不跑采集脚本。
> 本文档**取代**原 `P1评审文档_多奖项数据采集.md` 中"GFW 阻断、其他奖项仅策展"的前提 —— 实测海外站点现已基本全开。

---

## 0. 前提变更（实测，2026-08-01）

原 P1 文档（2026-07-31）判定：办公室电脑有 GFW，Wikidata 被墙、其他奖项官网不可达 → 只能"诺奖自动 + 其他策展"。

**本次实测（办公室电脑，代理/VPN 已开启，无需在脚本内配置代理地址）：**

| 数据源 | 实测 | 结论 |
|--------|------|------|
| Nobel v2 API `api.nobelprize.org/2.1/laureates` | **200**（带 UA） | ✅ 自动主源 |
| Google（对照组） | **200** | GFW 当前未阻断本机出口 |
| Wikidata SPARQL `query.wikidata.org` | **429**（限速，非阻断） | ✅ 可达，需节流 + 官方 UA + 失败重试 |
| IMU / Fields 官网 | 200 | ✅ 可采集 |
| Abel / Shaw / Kavli / Kyoto / Lasker / Wolf / Gruber / Crafoord / Gairdner / Breakthrough | **全部 200** | ✅ 可采集 |
| ACM Turing `amturing.acm.org` | **403**（WAF） | ⚠️ 需补浏览器头 / 换 Wikipedia+Wikidata 源 |
| `api.mathunion.org` | DNS 失败 | 非真实 API，忽略 |

**新约束（替代旧 GFW 约束）：**
- 采集期**必须保持代理/VPN 开启**（本机出口依赖它；脚本无需硬编码代理地址，直接走系统路由）。
- 海外可达性可能随代理状态波动 → 采集脚本必须**可重试、可断点续采、对 429/403 有退避**。
- 仍坚持**离线优先**：一次性采集落地本地文件，运行时零网络依赖（与 P0 内联 D3 一致）。

---

## 1. 背景与范围（同原 P1，沿用）

- 三体框架 = 物理 / 生命 / 计算；七行星安置；水滴探测器作探索者。
- 用户指令：「诺奖等科学边界相关的前沿领域的奖项都可以加上数据采集」。
- 本次范围：**诺奖（物理/化学/生理学或医学）自动采集 + 其他顶级科学奖项自动/半自动采集起步**。
- 本次**不涉及**：研究者档案视图（P2）、问题↔奖项关联自动补全（P2）、跨星系关联（P2）。

## 2. 数据源分级（按实测可达性 + 结构化程度）

### T0 · 自动主源（结构化 JSON API）
- **诺贝尔物理学/化学/生理学或医学奖**：`api.nobelprize.org/2.1/laureates`（v2，带 UA）→ 全量历史获奖者，字段完整。

### T1 · 官网可达、可采集（HTTP 200，需按站点结构解析）
| 奖项 | 站点 | 抓取方式 |
|------|------|----------|
| 菲尔兹奖 | mathunion.org | 官方名单页解析（4年一次，量小） |
| 阿贝尔奖 | abelprize.no | 历年列表页 |
| 图灵奖 | Wikipedia + Wikidata（避开 ACM 403） | 维基获奖者列表 → 反查 Wikidata Q-id |
| 邵逸夫奖（天文/生命/数学） | shawprize.org | 历年列表 |
| Kavli 奖 | kavliprize.org | 历年列表 |
| 京都奖（基础科学/生命科学/先进技术） | kyotoprize.org | 历年列表 |
| Lasker 奖 | laskerfoundation.org | 历年列表 |
| Wolf 奖（数学/物理/化学/医学） | wolffund.org.il | 历年列表 |
| Gruber 奖（宇宙学/基因/神经/正义） | gruber.yale.edu | 历年列表 |
| Crafoord 奖 | crafoord.se | 历年列表 |
| Gairdner 国际奖 | gairdner.org | 历年列表 |
| Breakthrough Prize（物理/数学/生命科学） | breakthroughprize.org | 历年列表 |

> 精选约 12–15 个奖项，避免过杂。T1 站点无统一 API，采用**每源一个轻量解析器**（HTML/JSON 提取），归一化到同一 schema。

### T2 · 统一富化层（Wikidata，429 限速需治理）
- 用途：为每个获奖者解析 **Q-id**，补充 `motivation` 中文摘要、`affiliation`、`country`、生卒年、Wikimedia 照片 URL、跨奖项关联。
- 治理：单线程 **1 req / 1.5–2s** + 官方 UA（`science-frontier-collector/1.0 (contact:...)`）+ 429 时指数退避（最长 60s）+ 失败项写入 `wikidata_failed.txt` 供人工补。
- 备选：Wikidata **REST 实体端点** `www.wikidata.org/wiki/Special:EntityData/Qxx.json`（比 SPARQL 端点更稳，优先用）。

## 3. 采集策略（升级版）

- **诺奖**：脚本自动拉 v2 API（带 UA），全量历史获奖者。
- **其他奖项（T1）**：每源一个解析器，自动/半自动拉取历年获奖者名单 + 基本字段；缺失字段（如中文摘要、机构）走 T2 Wikidata 富化。
- **富化（T2）**：以获奖者姓名 + 奖项年份为键，反查 Wikidata，补 `wikidata` Q-id 与可选字段。
- **离线优先**：所有数据落本地 `awards-data.js`，内联进 `index.html`，运行时零网络依赖。

## 4. 采集管线（脚本 `scripts/collect_awards.py` 升级版）

```
阶段1  各源拉取
  ├─ NobelFetcher    → 原始 JSON
  ├─ SiteFetcher[x]  → 各 T1 站点 HTML/JSON 解析（各自 parser）
  └─ 归一化到 awards schema（star/domain 映射）

阶段2  Wikidata 富化（限速 + 退避）
  ├─ 按 (name, year, award) 解析 Q-id
  ├─ 补 motivation/affiliation/country/photo/wikidata
  └─ 失败项写 wikidata_failed.txt

阶段3  合并 + 治理
  ├─ 科学边界过滤（仅 PHY/LIF/MAT/COS/ENG/INF/EAR/CHE/SOC/SPA 相关）
  ├─ 去重（同名跨奖项合并 links）
  ├─ 体积控制（见 §5）
  └─ 输出 website/awards-data.js（const awards = [...]）
```

- **断点续采**：每源中间结果落 `scripts/cache/<source>.json`；重跑时跳过已成功源；Wikidata 富化支持 `--resume`（只处理未富化项）。
- **可观测**：采集后打印「各奖项人数 / 各 star 分布 / 总条数 / Wikidata 富化成功率 / 失败项数」。

## 5. 体积与质量治理

- **筛选规则**：诺奖物理/化学/医学全收；其他奖项收 Apex/Elite 层代表性获奖者（每奖近 20–30 年或 Top N）。
- **上限**：目标 `awards` 总量 **≤ 1500 条**（诺奖约 1000 + 其他约 300–500）；超阈值时按年份截断其他奖项。
- **内联体积**：`awards-data.js` 优先内联进 `index.html`；若超 ~300KB，改为**同目录外链** + 预览服务器打开（`file://` 直开有旧加载问题）。
- **字段质量**：策展/解析数据人工抽检 5%；Wikidata 富化字段来源可追溯（保留 `wikidata` Q-id）。

## 6. awards 数据 Schema（沿用原 P1）

```js
const awards = [
  {
    id: "nobel-phys-2023",
    name: "Anne L'Huillier",
    year: 2023,
    award: "诺贝尔物理学奖",
    awardKey: "nobel-physics",
    star: "physics",            // physics / life / compute
    domain: "物理",
    motivation: "for ...",       // 中文摘要（Wikidata/官方）
    affiliation: "Lund University",
    country: "瑞典",
    photo: "https://upload.wikimedia.org/...",  // 可选，Wikidata 富化
    wikidata: "Q...",
    links: []                    // 关联 109 问题 id（P2 补全）
  },
  // ...
];
```

## 7. 文件与代码结构（沿用原 P1，扩展）

- **新增/升级** `science-frontier/scripts/collect_awards.py`（独立脚本，不碰 `index.html`）：
  - `NobelFetcher` + `SiteParser[source]` + `WikidataEnricher` + 合并/过滤/输出。
  - 子命令：`collect --source all|nobel|fields|...`、`enrich --resume`、`report`。
- **`index.html` 改动**（仅以下，均局部、可回退，同原 P1 §6）：
  1. `const laureates = []` → 引入 `awards` 数据
  2. 信任锚：`N 位灯塔照亮 109 问题`
  3. 新增「荣誉星图」视图（按 `star` 着色，hover 显获奖者/理由，点击进详情；窄屏回退网格）
  4. 配色复用 P0 语义色

## 8. 影响面与风险

- **影响面**：仅 `index.html` 单文件 + 新增独立脚本/数据文件/缓存目录。不动 P0 星系/水滴逻辑、不动 `window.DATA`、不动其他视图。
- **风险与缓解**：

| 风险 | 等级 | 缓解 |
|------|------|------|
| 代理/VPN 中途掉线 | 中 | 脚本可重试 + 断点续采；重跑自动续 |
| Wikidata 429 限流 | 中 | 节流 1 req/1.5–2s + 指数退避 + 失败清单人工补 |
| ACM Turing 403 | 低 | 改用 Wikipedia 名单 + Wikidata 反查，不直采 ACM |
| T1 站点改版致解析失败 | 中 | 每源 parser 独立；某源失败不影响其他源；失败源数据回退策展 |
| 数据量致 index.html 过大 | 低 | §5 体积控制；超限改外链 |
| 字段质量（解析/富化错漏） | 低 | 抽检 5% + 保留 Q-id 可追溯 |

- **回退**：`index.html.bak` 已备份（P0 留）；采集脚本独立；`awards` 数组可整体回退到 `[]`；缓存目录可清。

## 9. 验证方案

- jsdom 真实渲染：awards 渲染、荣誉星图节点数、信任锚数量、零 JS 错误。
- 本地 `http.server` 预览：结构完整、各 star 着色正确、窄屏回退。
- 数据校验：采集后打印「各奖项人数 / 各 star 分布 / 总条数 / 富化成功率」。
- 采集健壮性：模拟断网续跑、Wikidata 限速退避实测。

## 10. 明确不做（本次）

- 研究者档案视图（P2）
- 问题↔奖项 `links` 自动补全（P2）
- 跨星系关联扩展（P2）
- T1 站点持续监控/定时增量（P2，先一次性采集）

## 11. 签批栏

- [ ] 确认数据源分级（§2：T0 诺奖 / T1 官网自动 / T2 Wikidata 富化）
- [ ] 确认"代理开启即可直采、脚本不硬编码代理"前提（§0）
- [ ] 确认 awards schema（§6）
- [ ] 确认体积上限 ≤1500 条 / 内联优先（§5）
- [ ] 确认影响面与回退（§8）

签批：__________（签名 / 批准 / 修改意见）

---

### 附：实测证据
- 探测脚本：`scripts/_recon_sources.py`（stdlib，临时）
- 探测结果：`scripts/_recon_result.txt`（2026-08-01，办公室电脑 + 代理开启）
- 结论：原 P1 "GFW 阻断" 前提失效，本方案据此升级为代理增强版自动采集。
