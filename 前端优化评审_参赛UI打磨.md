# 前端展示优化评审文档 · 参赛 UI 打磨

> 配套：参赛规划 `参赛规划_小有可为2026.md`（Phase 1 初赛冲刺）
> 目标：参考阿里云「小有可为 2026」比赛站的展示手法，提升科学边界页面的前端展示效果，**贴合深空科普定位**（保留暗色星海风，不抄亮色）。
> 状态：**已签批 · 2026-08-01（用户"全部确认"）**。可实施（仅前端）。

---

## 1. 背景

- 用户判断当前页面「前端展示效果还不理想」，希望参考比赛站的好手法改进。
- 比赛站（opc.aliyun.com/xiaoyoukewei）是 SPA，WebFetch 只能取到文字结构，但已能提炼其**展示手法逻辑**。
- 核心原则：**借鉴「展示手法」（信息组织 / 信任感 / 引导 / 卡片交互），不抄「配色」（我们深空风更贴合星海定位）。**

---

## 2. 比赛站有效展示手法（提炼）

从页面结构提炼出 6 类可借鉴手法：

| # | 手法 | 比赛站怎么用 |
|---|------|------------|
| M1 | **金句式双层标题** | "两个月，做一件小事" + 副文案，首屏一句话说清价值 |
| M2 | **清晰区块标题 + 引导语** | 每个 section 有大标题，下面一句话说明"这是什么/怎么用" |
| M3 | **信任/权威模块** | 评委阵容、21万奖金、合作方、往届作品——建立可信度 |
| M4 | **案例/作品网格** | 往届作品缩略图网格，让抽象概念变具体 |
| M5 | **步骤化引导** | 报名/上手流程拆解成清晰步骤 |
| M6 | **卡片交互** | 大圆角、hover 反馈、柔和层次，降低认知负担 |

---

## 3. 我们页面当前短板（诊断）

| # | 短板 | 影响 |
|---|------|------|
| D1 | Hero 只有钩子句，缺"这是什么/谁该用"的副文案与行动按钮 | 首屏冲击力够但转化弱，访客不知下一步 |
| D2 | **缺信任模块** | 评委/案例→我们应有"数据可信度"，但当前只在统计条里有一行 |
| D3 | **缺探索引导** | 读者进星系后不知"先看什么" |
| D4 | 卡片 hover 动效偏弱 | 交互质感不如竞品 |
| D5 | 各视图缺 section 级标题与引导语 | 切换视图时读者容易"迷路" |
| D6 | 字体层级/间距可更精 | 长文本（问题详情）可读性可提升 |

---

## 4. 借鉴映射（手法 → 科学边界怎么用）

| 比赛站手法 | 科学边界借鉴（贴合深空定位） |
|-----------|---------------------------|
| M1 金句双层标题 | Hero 主标保留「站在已知的边缘」，加副文案："109 个最前沿科学问题，一座可交互的知识星座" + 两个按钮「开始探索」「这是什么」 |
| M2 区块标题+引导语 | 星系/列表/网络三视图各加 section 标题 + 一句引导语 |
| M3 信任模块 | 新增「为什么可信」徽章行：109/109 全溯源 · 诺奖荣誉星图筹备中 · 来源含论文/官方/图书 |
| M4 案例网格 | 领域星系本身即"案例墙"；问题详情强化"相关问题"关联卡片 |
| M5 步骤引导 | 新增「探索三步」横向步骤条：选领域 → 钻问题 → 追关联 |
| M6 卡片交互 | 卡片 hover 抬升 + 边框微亮 + 图标缩放；滚动进入渐显（IntersectionObserver） |

---

## 5. 具体改造项（仅前端）

### A. Hero 区强化
- 主标不变；新增副文案一行（说明价值）
- 新增两个行动按钮：`开始探索 →`（切到星系视图）、`这是什么？`（弹出科普说明浮层）
- 边界线 SVG 保留，加星辰缓动微光（克制，不抢内容）

### B. 信任锚模块（新增）
- Hero 下方一行三个徽章：`109/109 全溯源` · `诺奖荣誉星图筹备中` · `来源: 论文 / 官方 / 图书`
- 用轻量 SVG 图标 + 文字，深空配色（绿=可信、靛蓝=探索）

### C. 探索三步引导（新增）
- 横向步骤条卡片：① 选领域（点星系中的恒星/行星）② 钻问题（看影响力/难度/时间线）③ 追关联（力导向网络看知识关系）
- 每步配小图标，hover 高亮

### D. 卡片交互升级
- `.domain-card` / `.problem-card` hover：translateY(-4px) + 边框 `--accent` 微亮 + 图标 scale(1.08)
- 进入视口渐显（IntersectionObserver，仅宽屏，窄屏直接显示避免卡顿）

### E. 视图级引导语
- 每个视图切换时，顶部显示该视图的标题 + 一句引导语（如星系："按物理/生命/计算三柱展开的知识星座，拖拽恒星可聚焦某星系"）

### F. 字体与间距精修
- 标题字重统一、正文行高 1.7、移动端字号下限 14px
- 长文本（问题详情 description）限制最大宽度提升可读性

### G. （可选）滚动叙事节奏
- 首页从 Hero → 星系 → 信任 → 引导 的滚动流；若时间紧可放 Phase 2

---

## 6. 影响面与风险

- **影响面**：仅 `index.html` 的 CSS / HTML 结构（Hero 区、新增信任模块/引导条）/ 少量 JS（IntersectionObserver、按钮事件）。
- **不动**：`window.DATA` 数据层、D3 力导向核心逻辑、后端、其他视图数据。
- **风险与缓解**：
  - 视觉微调需多次预览 → 用本地 http.server 实时预览
  - 移动端兼容 → 窄屏保持网格回退，动效降级
  - 引入的 JS 需 jsdom 验证零错误
- **回退**：`index.html.bak` 已备份（P0 留）；各子项独立可回退。

## 7. 验证方案

- jsdom 真实渲染：零 JS 错误、新增模块渲染、按钮事件绑定
- 本地 `http.server` 预览：确认 Hero/信任/引导/卡片动效、窄屏回退
- 截图对比（优化前/后）

## 8. 明确不做

- 不抄亮色配色（保留深空星海风）
- 不引入前端框架（保持零依赖单文件）
- 不改数据层 / 后端 / D3 核心
- 不扩大为整站重构（仅参赛打磨，重架构留 P2）

## 9. 签批栏

- [x] 确认借鉴方向（手法不配色）
- [x] 确认改造项 A-G（或精简）
- [x] 确认影响面与回退
- [x] 确认先打磨参赛版（Phase 1）还是一并做 P1 诺奖

签批：**已批准 · 2026-08-01（用户"全部确认"）**。范围限定在 #22 前端动效；P1 多奖项（#15）不自动扩大。

---

## 10. 参考 motionsites.ai 的效果映射（新增 · 2026-08-01）

用户补充参考站 motionsites.ai（高端动效 Hero / 动画背景 / 交互组件提示词库，主打玻璃拟态、暗黑、3D、光标跟随、滚动触发、Loader、渐变光晕）。**结论：借「效果概念」，不借「代码」（其代码为 React/Tailwind/Framer Motion，我们是零依赖单文件，须用原生 CSS/JS 翻译）。**

| motionsites 效果 | 映射到科学边界 | 分类 | 说明 |
|---|---|---|---|
| 滚动触发渐显 | section/卡片进入视口 fade-up | ✅ 加法 | IntersectionObserver，仅宽屏；窄屏直显 |
| 玻璃拟态卡片 | 问题/领域卡 frosted blur + 边框微光 | ✅ 加法 | 保留语义色，仅叠加毛玻璃质感 |
| Hover 抬升 / 磁吸按钮 | 卡片 translateY + 按钮微磁吸 | ✅ 加法 | 呼应 §5-D |
| 极淡环境光晕 | hero/页脚低透明 aurora | ⚠️ 谨慎 | 必须置于星系之下、低透明度、不抢视觉；禁随机彩色渐变破坏 5 色语义 |
| 数字滚动计数 | 109/109 溯源、10 领域 等统计 | ✅ 加法 | 载入时数字滚动到位，强化信任锚 |
| 跑马灯 | 顶部/底部「热点领域」滚动条 | ✅ 加法 | 用现有 7 行星/10 领域名，纯展示流 |
| 粘性模糊导航 | 视图切换 tab 栏加 blur 背景 | ✅ 加法 | 升级现有 nav，保持深空 |
| Loader 动画 | 首屏品牌 Loader | ⚠️ 谨慎 | 可选；必须短、不替代内容 |
| 光标跟随 | — | 🛡 红线 | 已有水滴探测器，禁新增/替换光标效果 |
| 3D / Hero 大改 | — | 🛡 红线 | 不动 Hero 钩子句/边界线、不动三体星系结构 |
| 配色体系 | — | 🛡 红线 | 不动 5 色语义；不抄亮色/霓虹 |

## 11. 叙事保护红线（升级版 · 绝不动）

1. Hero 主标「站在已知的边缘，眺望未知的星海」+ 边界线 Boundary SVG：不动结构/文案。
2. 5 色语义系统（靛蓝紫/青/暖橙/红/绿）：不引入破坏语义的随机渐变/霓虹。
3. 三体星系视图（3 恒星 7 行星 D3 力导向）：不动结构。
4. 水滴探测器（光标跟随）：不叠加/替换其他光标效果。
5. 100% 溯源信任锚：不动数值与呈现。
6. 知识星座 / 探针光：保留为叙事高潮，优化仅限外围动效节奏。

## 12. 签批补充（ motionsites 参考）

- [x] 确认「借效果概念不借代码、零依赖单文件」原则
- [x] 确认 §10 加法层清单（或精简/增删）
- [x] 确认 §11 叙事保护红线（绝不动）

签批：**已批准 · 2026-08-01（用户"全部确认"）**。
